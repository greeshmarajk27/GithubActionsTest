name: Build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build stuff
        run: echo "Build done"

      - name: Add button link to summary
        run: |
          echo "## ✅ Build Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "[➡ Copy files to repo](https://github.com/${{ github.repository }}/actions/workflows/copy-files.yml)" >> $GITHUB_STEP_SUMMARY
